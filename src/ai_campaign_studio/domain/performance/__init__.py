"""Performance domain primitives (Slice 1.5, P1.5-G1).

Domain-only value objects and entities for the performance data that future
Slice 1.5 gates (Persistence, CSV import, matching) will persist and match.
No SQL, no I/O, no ports — just the vocabulary.
"""
