# ACS-F1-043 — Pi implementer evidence

Task: ACS-F1-043 — P1.5-G3 dio 2 -- CSV performance import: perzistencija
(`PerformanceImportRow`) + tri use-case-a (`ImportPerformanceCsv` /
`PreviewPerformanceMapping` / `ConfirmPerformanceImport`).

Agent: pi
Datum: 2026-09-06
Branch: `task/ACS-F1-043-csv-import-persistence` (commit `9ec5675`)
Worktree: `../ai-campaign-studio-worktrees/ACS-F1-043-csv-import-persistence`
Base: `main @ 223bf70` (kontrakt je naveo `4f4be8d`; stvarni main je već bio
na `223bf70` jer je koordinator u međuvremenu commit-ovao task contracts —
branch je baziran na stvarnom trenutnom main-u, ne na zastarjelom base-u).
PR: https://github.com/Rade69/AI-Campaing-Studio/pull/6

## Files changed (14, svi unutar allowed_paths)

```
resources/migrations/0007_performance_import_rows.sql                    (novo)
src/ai_campaign_studio/application/performance/__init__.py               (header)
src/ai_campaign_studio/application/performance/confirm_performance_import.py  (novo)
src/ai_campaign_studio/application/performance/import_performance_csv.py (novo)
src/ai_campaign_studio/application/performance/preview_performance_mapping.py (novo)
src/ai_campaign_studio/domain/common/ids.py                              (+1 ID tip)
src/ai_campaign_studio/domain/performance/entities.py                    (+entitet)
src/ai_campaign_studio/infrastructure/database/repositories/sqlite_performance_repository.py (+3 metoda)
src/ai_campaign_studio/ports/repositories.py                             (+3 port metoda)
tests/integration/application/performance/test_performance_import_flow.py (novo)
tests/unit/application/performance/test_confirm_performance_import.py     (novo)
tests/unit/application/performance/test_import_performance_csv.py         (novo)
tests/unit/application/performance/test_preview_performance_mapping.py    (novo)
tests/unit/infrastructure/database/repositories/test_sqlite_performance_repository.py (novo)
```

## Implementation summary

1. **Domain** — `PerformanceImportRow` (frozen dataclass, tačno po kontraktu:
   `id`, `batch_id`, `row_number`, `raw_values`, `mapped_values`, `errors`,
   `distribution_instance_id=None`) + `PerformanceImportRowId` u `ids.py`.
2. **Migracija `0007_performance_import_rows.sql`** — tačan sljedeći slobodan
   broj (provjereno `ls resources/migrations/` — zadnji postojeći `0006`).
3. **Repo** — 3 nova metoda na `PerformanceRepositoryPort` +
   `SqlitePerformanceRepository`: `save/get/list_performance_import_row`.
   JSON kolone (`raw_values_json`, `mapped_values_json`, `errors_json`),
   `list` je `ORDER BY row_number` (deterministički), `errors` se čuva kao
   JSON list i rekonstruiše u `tuple`.
4. **`ImportPerformanceCsv`** — jedino mjesto koje čita fajl sa diska
   (`csv.DictReader`, `encoding=utf-8-sig` zbog BOM, `newline=""`); vraća
   `(matches, parsed_rows)`; ne perzistuje.
5. **`PreviewPerformanceMapping`** — poziva import, vraća `PreviewResult`
   (kolona-mapping status po polju, valid/invalid brojevi, prvih 5 invalid
   redova sa `errors`); ne perzistuje.
6. **`ConfirmPerformanceImport`** — ponovo parsira (bez drugog čitanja diska:
   re-parse iz `ParsedRow.raw_values`), primjenjuje `column_overrides` PRIJE
   `parse_rows`, perzistuje 1 batch + 1 `PerformanceImportRow` po redu,
   `distribution_instance_id=None` za sve. `matched_count`=broj valid,
   `unmatched_count`=broj invalid (dokumentovana privremena semantika, u
   docstring-u i kontraktu).

## GitNexus pre-change (iz glavnog checkout-a, index fresh na 223bf70)

- `npx gitnexus status`: ✅ up-to-date (index commit 223bf70 == current 223bf70).
- `npx gitnexus impact PerformanceRepositoryPort --direction upstream`:
  risk MEDIUM, 18 impacted — SVE su `IMPORTS` relacije na modul
  `ports/repositories.py` (taj fajl sadrži sve portove; fajlovi ga importuju
  zbog drugih portova). Nijedan pozivalac ne koristi NOVE metode (ne postoje
  prije ovog diff-a).
- `npx gitnexus impact PerformanceImportBatch --direction upstream`:
  risk LOW, 3 direct importers (`repositories.py`, `export_campaign.py`,
  `sqlite_performance_repository.py`). Izmjena je aditivna — postojeći
  pozivaoci netaknuti.

## GitNexus detect-changes

Pokrenut iz worktree-a (`--scope compare --base-ref main --repo
AI-Campaing-Studio`): vratio poznati worktree-binding problem — rezultat
navodi pogrešne simbole (AGENTS.md/CLAUDE.md/presentation_webview) jer je
index bindovan na glavni checkout i 1 commit zastario. **Tretiram kao
UNKNOWN** (workflow §7, GITNEXUS_PROTOCOL §7) i kompenzujem ručno:

- `git diff main...HEAD --name-only` → tačno 14 fajlova, svi u
  `allowed_paths` (gore navedeni).
- Ručni caller grep za `PerformanceRepositoryPort`/`SqlitePerformanceRepository`:
  jedini produkcijski korisnik port-a je `export_campaign.py` (koristi samo
  postojeći `save_distribution_instance`, ne nove metode); implementacija je
  jedino `SqlitePerformanceRepository`. Pun suite (1009) je zelen → nema
  regresije u `export_campaign` integration testu.

Reviewer (Claude) treba da pokrene čist `detect-changes` poslije
fetch-a PR-a (branch je sada na origin-u, pa je dijafragma protiv stvarnog
task branch-a moguća iz glavnog checkout-a nakon `git fetch`).

## Verification (doslovan output)

```bash
cd worktree && PYTHONPATH=src python -m pytest tests/unit/domain/performance/ \
  tests/unit/infrastructure/database/repositories/test_sqlite_performance_repository.py \
  tests/unit/application/performance/ tests/integration/application/performance/ -v
# => 56 passed in 1.92s

PYTHONPATH=src python -m pytest -q
# => 1009 passed, 1 warning in 94.38s (0:01:34)

PYTHONPATH=src python -m ruff check .
# => All checks passed!

PYTHONPATH=src python -m mypy src
# => Success: no issues found in 174 source files
```

CI (GitHub Actions, PR #6, run 34045970072): ✅ zelen — ruff, mypy, pytest,
Validate bundled resources, No-secret scan, Health check svi ✓.

## Tests added

- `tests/unit/infrastructure/database/repositories/test_sqlite_performance_repository.py`
  — save/get/list round-trip, `ORDER BY row_number`, JSON round-trip sa BHS
  dijakriticima (`š`,`č`,`ć`,`š`,`đ`,`ž`), `distribution_instance_id=None`
  round-trip, scoping po batch-u, unknown→None.
- `tests/unit/application/performance/test_import_performance_csv.py` —
  stvaran tmp CSV, header+redovi, ispravan `ColumnMatch`/`ParsedRow`, BHS
  default rules, prazne ćelije→`""` (ne `None`), prazan fajl.
- `tests/unit/application/performance/test_preview_performance_mapping.py` —
  ambiguous kolona VIDLJIVA u sažetku, invalid redovi VIDLJIVI sa `errors`,
  ništa nije perzistovano (baza prazna poslije poziva).
- `tests/unit/application/performance/test_confirm_performance_import.py` —
  happy path round-trip preko repo (batch + svi redovi), `column_overrides`
  ispravlja ambiguous kolonu (bez override-a `spend` ostaje nemapiran; sa
  `{"spend": "cost"}` se mapira), invalid redovi se I DALJE perzistuju
  (3 ulazna → 3 perzistovana), `platform_code` se bilježi.
- `tests/integration/application/performance/test_performance_import_flow.py`
  — pun lanac import→preview→confirm nad stvarnim CSV sa MIJEŠANIM EN+BHS
  headerima i valid/invalid redovima; "ništa se ne gubi" (2 ulazna == 2
  perzistovana reda), BHS dijakritik očuvan do perzistovanog reda.

## OUT_OF_SCOPE_FINDINGS

1. **Putanja test fajla u kontraktu ne odgovara stvarnom repou.** Kontrakt
   navodi `tests/unit/infrastructure/database/repositories/test_sqlite_performance_repository.py`
   (u `allowed_paths` i u verification komandi), ali POSTOJEĆI performance
   repo test živi u `tests/integration/database/repositories/test_sqlite_performance_repository.py`
   (van `allowed_paths` ovog taska). Rješenje: napravio sam NOVI fajl tačno na
   kontraktovoj unit putanji sa row-testovima (u `allowed_paths`), bez diranja
   postojećeg integration test fajla. NIJE bilo potrebno mijenjati ništa van
   `allowed_paths`. (Postojeći integration repo test za
   DistributionInstance/Snapshot/Batch ostaje netaknut.)

2. **`Base: main @ 4f4be8d` u kontraktu je zastario** — main je bio na
   `223bf70` (task-contract commit). Branch baziran na `223bf70`, što je
   ispravno.

## Not verified

- Stvaran čist `gitnexus detect-changes` protiv task branch-a (worktree
  binding ograničenje — vidi gore; reviewer treba da ga pokrene poslije
  fetch-a).
- Nije pokretan `python scripts/check_no_secrets.py` niti
  `generate_phase0_gate_report.py` zasebno — ali CI (svjež checkout) je
  prošao `No-secret scan` i `Validate bundled resources`, a kontrakt ih ne
  traži eksplicitno u verification listi.
