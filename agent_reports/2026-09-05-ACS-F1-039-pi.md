# ACS-F1-039 — implementation evidence (Pi, 2026-09-05)

Implementer: Pi · Reviewer: Claude (MEDIUM, §29 Claude-only)
Branch: `task/ACS-F1-039-record-distribution-instance`
Worktree: `H:\ai-campaign-studio-worktrees\ACS-F1-039-record-distribution-instance`
Base: `main @ 3338c22` (kontrakt navodi `5162924`; `3338c22` = `5162924` +
ACS-F1-039 kontrakt markdown, bez source promjena)

ExportCampaign snima `DistributionInstance` po eksportovanom piece-u
(bridge P1.5-G2 → G3). MEDIUM risk.

Napomena: worktree NIJE bio pre-kreiran — kreirao sam ga prema kontraktovom
Worktree/Branch navodu.

## OUT_OF_SCOPE_FINDING — `tests/unit/ports/test_export.py` (van allowed_paths)

Kontrakt kaže: "Nema default vrijednosti (ExportResult se konstruiše SAMO
unutar ExportCampaign.execute() ... nema drugih pozivalaca koji bi se
pokvarili)." To NIJE tačno — `tests/unit/ports/test_export.py` PINUJE shape
`ExportResult` i konstruiše ga DIREKTNO (2 testa: `test_export_result_is_frozen`,
`test_export_result_fields_and_order`). Dodavanje obaveznog polja
`distribution_instance_ids` (bez default-a) je deterministički slomilo ta 2
testa (`TypeError: missing required argument`), što je dalje slomilo
`tests/unit/scripts/test_generate_phase0_gate_report.py` (gate report pokreće
`pytest -q` podproces koji prolazi kroz taj fajl).

**Rješenje**: ažurirao sam `tests/unit/ports/test_export.py` (mehanički: dodao
`distribution_instance_ids` u obje konstrukcije + `field_names` asert na 4
polja + docstring "four required fields"). Ovo je NUŽNA korekcija izvan
`allowed_paths` — kontrakt je propustio ovaj test fajl u popisu postojećih
pozivalaca. Prijavljujem transparentno, ne tiho.

## Files changed

1. `src/ai_campaign_studio/ports/export.py` (MODIFIED — `ExportResult` novo polje)
2. `src/ai_campaign_studio/application/export/export_campaign.py` (MODIFIED — dopuna)
3. `tests/unit/application/export/test_export_campaign.py` (MODIFIED — fake repo + 3 nova testa)
4. `tests/integration/application/export/test_export_campaign_integration.py` (MODIFIED — dopuna)
5. `tests/unit/ports/test_export.py` (MODIFIED — OUT_OF_SCOPE, vidi gore)

`domain/`, `ports/repositories.py`, `ports/rendering.py`, `infrastructure/`,
`resources/migrations/`, `application/{campaigns,posts,visual,rendering,evaluation}/`
— netaknuti.

## Implementation summary

### `ExportResult` + `distribution_instance_ids`

```python
@dataclass(frozen=True)
class ExportResult:
    zip_path: str
    exported_content_piece_ids: tuple[str, ...]
    skipped_content_piece_ids: tuple[str, ...]
    distribution_instance_ids: tuple[str, ...]  # NOVO, isti redoslijed
```

### `ExportCampaign.__init__` — nov OBAVEZAN parametar `performance_repo`

Dodat NA KRAJ liste parametara; `self._performance_repo = performance_repo`.

### U istoj petlji koja gradi `manifest_items` (gdje je `content_revision_id`
već izračunat) — za SVAKI eksportovani piece:

```python
distribution_instance = DistributionInstance(
    id=DistributionInstanceId(new_id()),
    campaign_id=campaign.id,
    campaign_item_id=e.piece.campaign_item_id,
    content_piece_id=e.piece.id,
    content_revision_id=RevisionId(content_revision_id),
    channel_code=e.piece.target.channel,
    platform_code=e.piece.target.platform_code,
    format_code=e.piece.target.format_code,
    distribution_source=DistributionSource.EXPORT,
    created_at=utc_now(),
)
self._performance_repo.save_distribution_instance(distribution_instance)
```

`external_account_id`/`external_content_id`/`published_at` ostaju `None`
(export ih ne zna). `ExportResult.distribution_instance_ids` = isti redoslijed.

`campaign.json`/`telemetry`/`content-NN`/`manifest.json` — NEPROMIJENJENI.

## Tests

Unit (`test_export_campaign.py`, 3 nova + fake):
- `test_distribution_instances_saved_for_each_exported_piece` — save 2×, polja
  ispravna, `content_revision_id` IDENTIČAN onom iz `manifest.json` ISTOG
  exporta (single calculation, ne dva odvojena), `distribution_source is EXPORT`.
- `test_export_result_distribution_instance_ids_match_order` — 2 unosa,
  isti redoslijed kao `saved`.
- `test_skipped_piece_gets_no_distribution_instance` — preskočen piece (bez
  payload) → save 1×, samo eksportovani.

Integration (`test_export_campaign_integration.py`, dopuna):
- pravi `SqlitePerformanceRepository`, round-trip `get_distribution_instance`
  vraća red; `content_revision_id` odgovara STVARNOJ Reviziji iz `revision_repo`.

## Verification (doslovan output)

Pokrenuto sa `PYTHONPATH=<worktree>/src` override (workflow §13 `.pth` zamka).

```text
$ python -m pytest tests/unit/application/export/ tests/integration/application/export/ -v
22 passed in 2.11s

$ python -m pytest -q
960 passed, 1 warning in 72.79s
   (1 warning = DeprecationWarning iz google.genai, van scope-a)

$ python -m ruff check .
All checks passed!

$ python -m mypy src
Success: no issues found in 168 source files
```

## git diff dokaz

```text
$ git status --short
 M src/ai_campaign_studio/application/export/export_campaign.py
 M src/ai_campaign_studio/ports/export.py
 M tests/integration/application/export/test_export_campaign_integration.py
 M tests/unit/application/export/test_export_campaign.py
 M tests/unit/ports/test_export.py
```

(`tests/unit/ports/test_export.py` = OUT_OF_SCOPE_FINDING iznad; sve ostalo u
allowed_paths.)

## GitNexus

Kontrakt: `gitnexus_required: true`, koordinator MORA pokrenuti impact na
`ExportCampaign` prije merge-a (mijenja se `__init__` potpis — nov obavezan
parametar). Očekivano nula produkcijskih pozivalaca (samo testovi), ali
provjera je obavezna, ne pretpostavka.

## Not verified

- Live export sa pravim ključem (nije potreban).
- `git commit` — MEDIUM, ne commit-ujem sam (§29).

## Worktree status (final)

```text
 M src/ai_campaign_studio/application/export/export_campaign.py
 M src/ai_campaign_studio/ports/export.py
 M tests/integration/application/export/test_export_campaign_integration.py
 M tests/unit/application/export/test_export_campaign.py
 M tests/unit/ports/test_export.py
?? agent_reports/2026-09-05-ACS-F1-039-pi.md
```
