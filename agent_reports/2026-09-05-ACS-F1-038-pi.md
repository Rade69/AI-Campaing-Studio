# ACS-F1-038 — implementation evidence (Pi, 2026-09-05)

Implementer: Pi · Reviewer: Claude (MEDIUM, §29 Claude-only)
Branch: `task/ACS-F1-038-performance-persistence`
Worktree: `H:\ai-campaign-studio-worktrees\ACS-F1-038-performance-persistence`
Base: `main @ a465585` (kontrakt navodi `c67447d`; `a465585` = `c67447d` +
ACS-F1-038 kontrakt markdown, bez source promjena)

P1.5-G2 Performance persistence — migracija + `PerformanceRepositoryPort` +
`SqlitePerformanceRepository`. MEDIUM risk.

Napomena: worktree NIJE bio pre-kreiran — kreirao sam ga prema kontraktovom
Worktree/Branch navodu.

## OUT_OF_SCOPE_FINDING — broj migracije 0004 je VEĆ ZAUZET

Kontrakt specificira `resources/migrations/0004_performance_foundation.sql`.
Ali `0004_uniqueness_constraints.sql` VEĆ POSTOJI (mergovana ranije — ACS-F1-022
"unique constraints" faza). Dva fajla sa version=4 bi, na svježoj bazi,
izazvala `sqlite3.IntegrityError: UNIQUE constraint failed:
schema_migrations.version` (oba pokušavaju upisati version=4).

**Rješenje**: koristio sam sljedeći slobodan broj —
`resources/migrations/0006_performance_foundation.sql` (postojeći redoslijed:
0000 foundation, 0001 brand_facts, 0002 campaign_content_visual, 0003
content_payload, 0004 uniqueness_constraints, 0005 layout_specs). DDL je
IDENTIČAN onom iz kontrakta, samo broj fajla je 0006 umjesto 0004.

Ovo je kontrakt-korekcija, ne širenje scope-a — `forbidden_paths` kontrakta
ne pominje `0004_uniqueness_constraints.sql` (koordinatorov propust u popisu
postojećih migracija). Prijavljujem transparentno umjesto da tiho biram broj.

## Files changed

1. `resources/migrations/0006_performance_foundation.sql` (NEW — DDL identičan
   kontraktovom, samo broj 0006)
2. `src/ai_campaign_studio/ports/repositories.py` (MODIFIED — nov port, aditivno)
3. `src/ai_campaign_studio/infrastructure/database/repositories/sqlite_performance_repository.py` (NEW)
4. `src/ai_campaign_studio/infrastructure/database/repositories/__init__.py` (MODIFIED — re-export)
5. `tests/integration/database/repositories/test_sqlite_performance_repository.py` (NEW)
6. `tests/integration/database/test_migrations.py` (MODIFIED — dopuna)

Sve unutar `allowed_paths`. `domain/`, `application/`, migracije `0000`-`0005`,
`ports/ai.py`, `ports/rendering.py`, `ports/export.py` — netaknuti.

## Implementation summary

### Migracija (3 tabele, redoslijed po FK zavisnosti)

`performance_import_batches` → `distribution_instances` (FK na campaigns/
campaign_items/content_pieces/revisions) → `performance_snapshots` (FK na
distribution_instances + nullable performance_import_batches). 9 canonical
metrika kao zasebne kolone, `raw_metrics_json` TEXT. Bez `ON DELETE CASCADE`.
`performance_import_rows` NAMJERNO IZOSTAVLJENA (P1.5-G3).

### `PerformanceRepositoryPort` — minimalan (6 metoda, samo save/get po id-u)

Aditivan; postojeći portovi netaknuti. Bez query-po-vezanom-entitetu metoda
(niko ih još ne traži).

### `SqlitePerformanceRepository`

- save/get za sva tri entiteta; upsert `INSERT ... ON CONFLICT(id) DO UPDATE`.
- enum → `.value` pri snimanju, `EnumType(row[...])` pri čitanju.
- `MetricPeriod`/`CanonicalMetricSet` rekonstruisani iz flat kolona;
  `raw_metrics` kroz `json.dumps`/`json.loads`; `datetime.fromisoformat`.
- `performance_snapshots` upsert je običan (nema ACS-F1-030-stil created_at
  izuzetak — `observed_at` je domain polje, ne adapter-generisan timestamp).

## Tests (7 novih + 1 dopuna)

`test_sqlite_performance_repository.py` (7):
- isinstance port; round-trip sva tri entiteta (enum + nested value objekti);
- snapshot sa svim None metricama; unknown → None za sva tri;
- `raw_metrics={"instagram_saves": 42}` round-trip;
- FK constraint test (`PRAGMA foreign_keys=ON`, save bez seed-a →
  `sqlite3.IntegrityError`).

`test_migrations.py` (1 dopuna): `6 in applied`, 3 tabele postoje,
`schema_migrations` ima red za 6.

## Verification (doslovan output)

Pokrenuto sa `PYTHONPATH=<worktree>/src` override (workflow §13 `.pth` zamka).

```text
$ python -m pytest tests/integration/database/repositories/test_sqlite_performance_repository.py tests/integration/database/test_migrations.py -v
15 passed in 1.83s

$ python -m pytest -q
957 passed, 1 warning in 83.22s
   (1 warning = DeprecationWarning iz google.genai, van scope-a)

$ python -m ruff check .
All checks passed!

$ python -m mypy src
Success: no issues found in 168 source files
```

## git diff dokaz — nema izmjena van allowed_paths

```text
$ git status --short
 M src/ai_campaign_studio/infrastructure/database/repositories/__init__.py
 M src/ai_campaign_studio/ports/repositories.py
 M tests/integration/database/test_migrations.py
?? resources/migrations/0006_performance_foundation.sql
?? src/ai_campaign_studio/infrastructure/database/repositories/sqlite_performance_repository.py
?? tests/integration/database/repositories/test_sqlite_performance_repository.py
```

## Not verified

- Nema live/API promjena (čista SQLite migracija + adapter).
- `git commit` — MEDIUM, ne commit-ujem sam (§29).

## Worktree status (final)

```text
 M src/ai_campaign_studio/infrastructure/database/repositories/__init__.py
 M src/ai_campaign_studio/ports/repositories.py
 M tests/integration/database/test_migrations.py
?? resources/migrations/0006_performance_foundation.sql
?? src/ai_campaign_studio/infrastructure/database/repositories/sqlite_performance_repository.py
?? tests/integration/database/repositories/test_sqlite_performance_repository.py
?? agent_reports/2026-09-05-ACS-F1-038-pi.md
```
