# ACS-F1-031 — implementation evidence (Crush, 2026-09-05)

Implementer: Crush · Reviewer: Claude
Branch: `task/ACS-F1-031-plan-post-layout`
Worktree: `H:\ai-campaign-studio-worktrees\ACS-F1-031-plan-post-layout`
Base: `main @ 30f66e5`

A13 dio 2b — per-post `LayoutSpec` + headline-fit validacija. Napomena: worktree
nije bio kreiran (kontrakt implementer = "TBD"), kreirao sam ga iz main @ 30f66e5.

## Novi fajlovi

1. `resources/prompts/post_layout/v1.yaml` — koristi postojeći
   `LayoutSpecCandidate` kao `output_contract`.
2. `application/visual/validate_layout.py` — čista, deterministička, bez I/O:
   `validate_layout(layout, headline) -> (bool, reasons)`; HERO (max 55) /
   SPLIT (max 48) headline fit, granica `<=` je `True`. CTA slot van scope-a.
3. `application/visual/plan_post_layout.py` — `PlanPostLayout` use-case.
4. Testovi (2 unit + 1 integration).

## Dizajn odluke (dokumentovano)

- **Primitiv-pripadnost**: dozvoljeni skup = `{primary_layout_family}` +
  `secondary_layout_family` (ako nije `None`) — NE ceo `LayoutPrimitive` enum.
  Primitiv van skupa → `InvariantViolation`, ništa se ne perzistuje.
- **`format` override**: `_SLICE1_FORMAT = "1080x1350"` uvek, bez obzira šta AI
  vrati (test sa AI koji vrati `"999x999"` dokazuje).
- **Headline-fit NIJE fatalan**: predug headline se perzistuje sa
  `validation_status="INVALID"`, za razliku od primitiv-pripadnosti koja baca —
  namerna asimetrija.
- **`validate_layout` NE konstruiše `ContentSlotContract`** (`domain/visual/slots.py`)
  — plan sekcija 41 ne daje `bounding_box`/`font_family`/itd.; koristi lokalnu
  `_HeadlineLimits` strukturu.

## CI verifikacija

| Provjera | Rezultat |
|---|---|
| pytest (visual) | **27 passed** |
| pytest (full) | **865 passed** |
| ruff | All checks passed |
| mypy | Success (151 source files) |

## Scope

```
?? resources/prompts/post_layout/
?? src/ai_campaign_studio/application/visual/plan_post_layout.py
?? src/ai_campaign_studio/application/visual/validate_layout.py
?? tests/integration/application/visual/test_plan_post_layout_integration.py
?? tests/unit/application/visual/test_plan_post_layout.py
?? tests/unit/application/visual/test_validate_layout.py
```

`domain/`, `ports/`, `infrastructure/`, `application/schemas/`,
`resources/migrations/`, `application/visual/generate_visual_system.py` —
**netaknuti** (git diff dokaz). `LayoutSpecCandidate` reuse, bez novog schema.

Nisam commit-ovao (§29 — Claude review, PASS → merge).
