# ACS-F1-023 — implementation evidence (Crush, 2026-09-04)

Implementer: Crush · Reviewer: Claude
Branch: `task/ACS-F1-023-unique-constraints`
Worktree: `H:\ai-campaign-studio-worktrees\ACS-F1-023-unique-constraints`
Base: `main @ a62fd5a`

Nova append-only migracija `0004_uniqueness_constraints.sql` dodaje dva UNIQUE
indeksa (SQLite nema `ALTER TABLE ... ADD CONSTRAINT`).

## Izmjene

### `resources/migrations/0004_uniqueness_constraints.sql` (nov)

```sql
CREATE UNIQUE INDEX idx_revisions_entity_version
    ON revisions(entity_type, entity_id, version);

CREATE UNIQUE INDEX idx_campaign_items_plan_order
    ON campaign_items(plan_id, "order");
```

### `tests/integration/database/test_uniqueness_constraints.py` (nov)

- `test_migration_applies_unique_indexes` — `version 4` u `schema_migrations`,
  oba indeksa prisutna (`PRAGMA index_list`).
- `test_revisions_duplicate_entity_version_raises` — dva reda isti
  `(entity_type, entity_id, version)` (različit `id`) → `sqlite3.IntegrityError`.
- `test_campaign_items_duplicate_plan_order_raises` — dva reda isti
  `(plan_id, order)` (različit `id`) → `sqlite3.IntegrityError`.

## Scope napomena (putanja u kontraktu)

Kontrakt `allowed_paths` navodi `tests/integration/infrastructure/database/`,
ali taj direktorijum NE postoji — stvarni je `tests/integration/database/`
(gdje je postojeći `test_migrations.py`). Test dodat u stvarno mjesto.

## CI verifikacija

| Provjera | Rezultat |
|---|---|
| pytest (migrations + uniqueness) | **9 passed** |
| pytest (full) | **734 passed** (0 regresija — novi indeksi nisu otkrili duplikate u postojećim fixture-ima) |
| ruff | All checks passed |
| mypy | Success (139 source files) |

## Scope

```
?? resources/migrations/0004_uniqueness_constraints.sql
?? tests/integration/database/test_uniqueness_constraints.py
```

`0000`-`0003` migracije, `domain/`, `application/`, `ports/`, repository fajlovi
— **netaknuti** (git diff dokaz). Postojeći `save_revision` (`ON CONFLICT(id)`)
i `save_plan` (delete-and-reinsert) netaknuti; novi indeksi su nezavisan safety
net i ne mijenjaju upsert ponašanje.

Nisam commit-ovao (§29 — Claude review, PASS → merge). Koordinator će nezavisno
primijeniti migraciju protiv svoje postojeće dev baze prije merge-a (review focus).
