---
task: ACS-F1-056 — P1.5-G8 Integration acceptance (posljednji Slice 1.5 gate)
implementer: pi
worktree: ../ai-campaign-studio-worktrees/ACS-F1-056-g8-integration-acceptance
branch: task/ACS-F1-056-g8-integration-acceptance
base: main @ 4b0ad68 (uključuje ACS-F1-057 merge)
status: IMPLEMENTED (čeka review)
---

# Implementer evidence — ACS-F1-056

## Task

Deterministički E2E test cijelog Performance lanca preko bridge-a. Test-only,
nula produkcijskog koda — dokazuje ožičenje (ne AI kvalitet). Ovo je
posljednji gate Slice 1.5; nakon PASS-a Slice 2 (Website Ingestion) može krenuti.

## Files changed

```text
tests/integration/presentation_webview/bridge/test_campaign_bridge_g8_acceptance.py  (NOV)
```

Tačno 1 fajl, sve u `allowed_paths`; `src/` i `resources/migrations/` NETAKNUTI
(`git status` potvrđuje — nula M/A na `src/`).

## Implementation summary

Deterministički fake `TextGenerationPort` (`_FakeAiAdapter`) dispatch-uje na
`AIRequest.purpose` sa STVARNIM vrijednostima iz produkcijskog koda:
`campaign_plan` → `CampaignPlanOutput` (2 itema), `post_generation` →
`SocialPostGenerationOutput` (jedinstven headline po pozivu),
`visual_direction` → `VisualDirectionOutput`, `post_layout` →
`LayoutSpecCandidate`. Neiznenađujući purpose → `AssertionError` (dispatch je
dokaz, ne nagađanje).

`build_text_generation_adapter` potpuno patch-ovan
(`...bridge.build_text_generation_adapter`) + `secret_store.get_secret`
patch-ovan na placeholder — NULA stvarnog mrežnog/AI poziva, test radi bez
ijednog API ključa u okruženju (potvrđeno sa `AI_CAMPAIGN_STUDIO_DEEPSEEK_API_KEY`
unset).

Scenario (jedan test, sekvencijalno): create+plan → generate content
(job-backed, `_wait_for_job_terminal`) → export (manifest) → izvući
`analytics_match_key` po stavci → sintetički CSV (2 reda sa TAČNIM
ključevima) → `confirm_performance_import` (import+match+materialize u jednom
pozivu) → `get_campaign_performance` (agregat) →
`get_campaign_content_performance` (redovi).

## Task-definišući dokaz (ručno izračunate vrijednosti)

- `matched_count == 2` (SVI CSV redovi poklopljeni — `analytics_match_key`
  lanac export→manifest→CSV→match je STVARAN, ne mock-ovan).
- `materialized_count == 2`, `skipped_invalid_count == 0`.
- `distribution_instance_count == 2`.
- derived iz ručno izračunate sume (impressions 3000, clicks 102, spend 600,
  conversions 15, revenue 1500): ctr≈102/3000, cpc≈600/102, cpm≈200,
  cpa≈40, roas≈2.5, conversion_rate≈15/102.
- content redovi: 2 reda, `content_piece_id` skup == manifest skup, svaki red
  ima `ctr`/`cpc` != None (materializacija radi).

## Verification — DOSLOVAN output

```text
$ python -m pytest tests/integration/presentation_webview/bridge/test_campaign_bridge_g8_acceptance.py -v
1 passed in 4.74s

$ (3x zaredom, AI_CAMPAIGN_STUDIO_DEEPSEEK_API_KEY unset)
1 passed in 1.57s
1 passed in 2.46s
1 passed in 1.37s

$ python -m pytest -q   (AI_CAMPAIGN_STUDIO_DEEPSEEK_API_KEY unset)
1224 passed, 1 skipped, 1 warning in 221.59s

$ python -m ruff check .
All checks passed!

$ python -m mypy src
Success: no issues found in 178 source files
```

## Determinism / no-network dokaz

Test je deterministički: fake adapter vraća fiksne payload-e, nema mreže, nema
env var zavisnosti, `get_secret` patch-ovan. 3x zaredom identičan PASS. Jedini
"random" dio su `new_id()` UUID-jevi, koji NE ulaze ni u jednu aserciju
(asercije su count/skup/derived, ne konkretni ID stringovi).

## OUT_OF_SCOPE_FINDINGS

Nema.

## Not verified

- Stvarni `gh pr checks` (CI) — PR još nije kreiran u trenutku pisanja.
- GitNexus `detect-changes` iz worktree-a (test-only task; kompenzovano
  ručnim `git status` — samo 1 novi fajl, src netaknut).
