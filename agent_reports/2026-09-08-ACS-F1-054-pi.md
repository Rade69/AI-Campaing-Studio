---
task: ACS-F1-054 — Pregled i izvoz ekran prikazuje performance po pojedinom content piece-u
implementer: pi
worktree: ../ai-campaign-studio-worktrees/ACS-F1-054-content-performance-table
branch: task/ACS-F1-054-content-performance-table
base: main @ ef9bd1e (stvarni main HEAD u trenutku grananja: d566f68 — dodat CI-only commit između)
status: IMPLEMENTED (čeka review)
---

# Implementer evidence — ACS-F1-054

## Task

P1.5-G7b (revidiran): content-piece-level performance tabela unutar postojeće
G7a "Učinak kampanje" kartice u "Pregled i izvoz" ekranu, koristeći SAMO
postojeći `campaign_id` identitet preko `list_campaign_content` +
`build_content_performance_summary` (oba postoje iz ranijih taskova).

## Files changed (sve u allowed_paths, 0 forbidden)

```text
src/ai_campaign_studio/presentation/ui_models.py                 (+36)
src/ai_campaign_studio/presentation/contracts.py                 (+5)
src/ai_campaign_studio/presentation_webview/bridge/__init__.py   (+105)
src/ai_campaign_studio/presentation_webview/screens/pregled_izvoz/__init__.py (+61)
src/ai_campaign_studio/presentation_webview/static/app.js        (+81)
tests/unit/presentation/test_ui_models.py                        (+116)
tests/unit/presentation/test_contracts.py                        (+23)
tests/unit/presentation_webview/bridge/test_campaign_bridge_api.py (+188)
tests/unit/presentation_webview/test_pregled_izvoz_ssr.py        (+222)
tests/unit/presentation_webview/test_static_pages_generator.py   (+24)
```

10 fajlova, 859 insertions / 2 deletions (2 deletions = import-linije
proširene, ne brisanja). `git status --porcelain` ne pokazuje untracked
source fajlove — samo ovaj report (ne commit-uje se).

## Implementation summary

1. **Bridge** — nova READ js_api metoda
   `get_campaign_content_performance(raw_payload) -> dict`:
   - boundary validacija (dict + `campaign_id` str non-empty),
   - nepostojeća kampanja → `VALIDATION_ERROR`,
   - `list_campaign_content(campaign_id)` → za svaki piece
     `build_content_performance_summary(performance_repo, piece.id)` (jednom
     po piece-u, ne dvaput),
   - `derived.ctr`/`derived.cpc` po redu ISKLJUČIVO iz G6 buildera (podskup
     CTR+CPC — pun 6-metrički set već je na G7a campaign kartici),
   - label = `platform_code/format_code` + ` — {payload.headline}` samo kad
     payload postoji; bez payload-a → goli platform/format (ne prazan string),
   - nula content piece-ova → `ok=True` + prazan `rows` (ne greška).
   - Novi `_LIFECYCLE_ERROR_MAPPERS`/`_LIFECYCLE_ERROR_MESSAGES` ulaz +
     dedikovani `_content_performance_err` (exact DTO shape, bez leak-a
     tuđih ključeva).
2. **ui_models** — `ContentPerformanceRowUiModel` (content_piece_id, label,
   ctr, cpc) + `ContentPerformanceResultUiModel` (ok, rows, error_code,
   error_message). Frozen, JSON-safe, bez secret polja.
3. **contracts** — `PresentationFacade.get_campaign_content_performance`
   potpis.
4. **pregled_izvoz SSR** — nova kartica `data-content-perf-card` ("Učinak po
   objavi") u istom `grid g2` redu kao `data-perf-card`, tabela
   `data-content-perf-table` sa `data-content-perf-rows` tbody +
   `data-content-perf-note`. SSR placeholder: 2 reda iz DEFAULT_FIXTURE,
   prazna fixture → "Nema podataka." red. Label-i escape-ovani kroz
   `html.escape` (XSS).
5. **app.js** — novi `loadContentPerformance()` IIFE:
   - **`campaign_id` REUSE-OVAN** iz shared `appCampaignId` (parsiran JEDNOM
     u boot IIFE-u, linija 599) — Codex F1-053 napomena PRIMIJENJENA, ne
     ponovljen `URLSearchParams` (grep dokaz: samo boot IIFE + postojeći
     G7a IIFE na liniji 876 sadrže `new URLSearchParams`; novi IIFE koristi
     `appCampaignId`),
   - immediate fast path + `pywebviewready` `{once:true}`,
   - ekran-specifičan `[data-content-perf-table]` guard (nula dodira drugih
     ekrana),
   - label/headline kroz `escapeHtml` (XSS), CTR/CPC kroz `escapeHtml(fmt())`
     (brojevi safe ali defanzivno), `None` → 'N/A'.

## GitNexus pre-change evidence

- `npx gitnexus status` (iz glavnog checkout-a, `H:\AI Campaing Studio`):
  `Indexed: 8.9.2026 16:13:55`, `Status: ✅ up-to-date`.
- `npx gitnexus impact build_content_performance_summary --direction upstream`:
  impactedCount 0, risk LOW (moj bridge je PRVI caller — potpis
  `build_content_performance_summary(repo, content_piece_id)` potvrđen
  čitanjem source-a, nepromijenjen od F1-050).
- `npx gitnexus impact CampaignBridgeApi --direction upstream`:
  1 direktni importer (`presentation_webview/__main__.py`), risk LOW.
- `list_campaign_content(campaign_id)` potpis potvrđen čitanjem
  `sqlite_content_repository.py` (ORDER BY content_pieces.id — stabilan
  redoslijed redova).
- Worktree-binding ograničenje (workflow §7): `detect-changes` iz task
  worktree-a nije pouzdan; kompenzovano ručnim `git diff --stat` (10 fajlova,
  svi allowed_paths) + `rg`/čitanje stvarnih callera.

## Verification — DOSLOVAN output

```text
$ node --check src/ai_campaign_studio/presentation_webview/static/app.js
(bez outputa → exit 0, NODE_CHECK_OK)

$ python -m pytest tests/unit/presentation/ tests/unit/presentation_webview/ -q
353 passed in 31.49s

$ python -m pytest -q
1199 passed, 1 warning in 188.11s (0:03:08)
(warning = pre-existing google.genai Python 3.17 deprecation)

$ python -m ruff check .
All checks passed!

$ python -m mypy src
Success: no issues found in 177 source files

$ python scripts/check_no_secrets.py
NO CONFIRMED SECRET IN TRACKED FILES
```

## Adversarial proof (mutation-test, izvršni Node/VM)

Novi test `test_app_js_content_performance_hydration_lifecycle_isolation_and_xss`
pokreće stvarni commitovani `app.js` u Node/VM sa DOM double koji modelira
`addEventListener` options i emituje `pywebviewready` DVAPUT. Dvije mutacije
primijenjene privremeno (preko Edit alata, fajl vraćen identično — diff protiv
backup-a prazan):

1. **uklonjen `{once:true}`** → test FAIL:
   `{'lateApiCalls': 2} != {'lateApiCalls': 1}`,
   `{'lateListenerCleared': False} != {'lateListenerCleared': True}`.
2. **uklonjen `escapeHtml(r.label)`** (label sirov u `innerHTML`) → test FAIL:
   `{'lateEscaped': False} != {'lateEscaped': True}`.

Oba FAIL-a potvrđuju da test stvarno razlikuje ispravnu implementaciju od
poznatih regresija (exactly-once + XSS). Ispravan kod → PASS.

## Tests added/changed

- `tests/unit/presentation/test_ui_models.py`: 6 novih (row shape, success
  shape, empty success, error shape, no-secret).
- `tests/unit/presentation/test_contracts.py`: `get_campaign_content_performance`
  u `_EXPECTED_METHODS` + `test_bridge_implements_get_campaign_content_performance`.
- `tests/unit/presentation_webview/bridge/test_campaign_bridge_api.py`:
  `_seed_content_performance` helper + 5 testova (nepostojeća kampanja, bez
  piece-ova → prazno, >=2 piece-a sa različitim performance stanjima + fallback
  label bez payload-a, non-dict, no-secrets).
- `tests/unit/presentation_webview/test_pregled_izvoz_ssr.py`: 3 SSR testa
  (markeri, prazna fixture, XSS escape fixture labela) + izvršni Node/VM test.
- `tests/unit/presentation_webview/test_static_pages_generator.py`:
  content-performance markeri u build-time statičkom HTML.

## OUT_OF_SCOPE_FINDINGS

Nema.

## Not verified

- Stvarni `gh pr checks` (CI) — PR još nije kreiran u trenutku pisanja ovog
  reporta; CI se provjerava poslije push-a.
- GitNexus `detect-changes` iz worktree-a (poznato binding ograničenje,
  kompenzovano ručnim diff pregledom).
