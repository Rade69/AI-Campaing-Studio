# ACS-P0-002 — implementer/execution evidence (coordinator-confirmed)

**Task:** ACS-P0-002 — Config/logging/common + architecture boundaries (P0.06–P0.10)
**Implementer:** Pi — predao vlastiti detaljan report:
`agent_reports/2026-08-30-ACS-P0-002-pi.md` (u worktree-u, nekomitovan van
`allowed_paths` — pročitan i sadržajno potvrđen od strane koordinatora)
**Branch/worktree:** `task/ACS-P0-002-config-boundaries`,
`../ai-campaign-studio-worktrees/ACS-P0-002-config-boundaries`
**Commit:** `c6fa0b8` (author Pi, committed by coordinator — implementeri ne
commit-uju sami)

## Napomena implementera (proslijeđeno preko Human Ownera)

1. `agent_reports/ACS-P0-002-task-contract.md` nije postojao u worktree-u jer
   je koordinator commit-ovao kontrakt na `main` NAKON što je worktree
   branch-ovan sa `main@1725aaa`. Očekivano git ponašanje (task contract nije
   u `allowed_paths` implementera), Pi je ispravno pročitao kontrakt iz
   glavnog repo foldera.
2. Pi je koristio lokalni `.venv` u worktree-u (gitignored), Python 3.14.1,
   editable install — isti obrazac kao ACS-P0-001.

## Files changed — nezavisno potvrđeno (`git status --short` prije commit-a)

Identično onome što Pi navodi u svom reportu: sve unutar `allowed_paths`
(`config.example.toml`, `__init__.py`, `bootstrap.py`, `main.py`,
`config/{__init__,settings,paths}.py`, `logging/{__init__,config,events,
redaction}.py`, `domain/{__init__}.py`, `domain/common/{__init__,ids,errors,
timestamps}.py`, `application/__init__.py`, `ports/__init__.py`,
`presentation/__init__.py`, `tests/test_foundation.py`,
`tests/architecture/test_import_boundaries.py`, `tests/unit/config/*.py`,
`tests/unit/logging/test_redaction.py`, `tests/unit/domain/test_common.py`).
Nijedan `forbidden_path` (`domain/brand|campaign|content`, `infrastructure/`,
`localization/`, `channels/`, `ai_registry/`, `jobs/`, `presentation_qt/`,
`presentation_webview/`) nije dirnut — potvrđeno.

## Verification commands — DOSLOVAN output (nezavisno ponovljeno)

```text
$ ./.venv/Scripts/python.exe -c "import ai_campaign_studio; print(ai_campaign_studio.__version__)"
0.1.0

$ ./.venv/Scripts/python.exe -m pytest -q
..............................                                           [100%]
30 passed in 0.50s

$ ./.venv/Scripts/python.exe -m ruff check .
All checks passed!

$ ./.venv/Scripts/python.exe -m mypy src
Success: no issues found in 18 source files

$ ./.venv/Scripts/python.exe -m ai_campaign_studio.main --health-check
exit code 0

$ ./.venv/Scripts/python.exe -m pip list --format=freeze | grep -iE "pyside|pyqt|webview|playwright|^openai|^anthropic|google-generativeai|deepseek|^flask|^fastapi"
(no matches)
```

## Adversarial proof — nezavisno ponovljeno

Boundary checker: dodat `src/ai_campaign_studio/domain/_adv_check.py` sa
`import PySide6`:

```text
$ python -m pytest -q tests/architecture/test_import_boundaries.py::test_real_tree_has_no_boundary_violations
F [100%]
AssertionError: assert ["domain/_adv...rt 'PySide6'"] == []
1 failed
```

Nakon uklanjanja fajla:

```text
$ python -m pytest -q tests/architecture/test_import_boundaries.py
.. [100%]
2 passed
```

FAIL→PASS potvrđen nezavisno, poklapa se sa Pi-jevim reportom. Redaction
FAIL→PASS proof (no-op `redact()`) nije ponovo ručno izvršen od strane
koordinatora — Pi-jev doslovan output (9 failed → 11 passed) je pregledan i
konzistentan je sa testovima koje sam pročitao u
`tests/unit/logging/test_redaction.py` (parametrizovan test sa 8 key-name
varijanti + nested containers, realno bi pao na no-op implementaciji).

## GitNexus

`detect-changes --scope compare --base-ref main` pokrenut iz glavnog
checkout-a vraća diff GLAVNOG worktree-a (nekomitovane Performance/Analytics
izmjene), ne task branch — GitNexus binduje "repo" na registrovani glavni
checkout, ne na linked worktree (isti poznati caveat kao u Pi-jevom reportu i
`.agent/GITNEXUS_PROTOCOL.md` §7). Automatski `detect-changes` za ovaj task
NIJE dobijen — tretirano kao `UNKNOWN`, ne kao "nema impacta" (protokol §5).

Kompenzovano ručnim pregledom kompletnog diff-a fajl-po-fajl (svi izvorni
fajlovi pročitani u cjelini od strane koordinatora) plus pre-impact analizom
iz Task Contracta (`bootstrap.py`/`create_bootstrap`/`main` upstream risk
LOW, 2 callera) — stvaran diff ostaje unutar najavljenog obima, nema novih
poziva u module van `allowed_paths`.

## Acceptance checklist

Svih 12 stavki iz Task Contracta — PASS, potvrđeno nezavisno (vidi verifikacione
komande iznad + čitanje izvornog koda).

## Arhitekturne opservacije (ne blocking, za review)

- `AppPaths._default_resources_dir()` koristi `Path(__file__).resolve().parents[3]`
  da nađe repo-root `resources/` — pretpostavlja source-tree/editable-install
  layout. Radi za P0, ali će trebati revizija kad/ako paket ikad bude
  distribuiran kao instalirani wheel bez bundle-ovanih `resources/` uz njega
  (npr. `importlib.resources`). Nije u scope-u ovog taska da se to rješava.
- `main.py` hvata `except Exception` generalno oko `create_bootstrap()`. Ne
  guta grešku (vraća 1, ne 0) i ne loguje/printa exception detalje — prihvatljivo
  za P0 health-check entrypoint, ali future health-check UI/CLI će vjerovatno
  htjeti strukturiran error output (koristeći `AppError`/`ErrorCode` iz ovog
  istog taska) umjesto golog exit koda.

## Not verified

- GitNexus automated `detect-changes` (worktree-binding limitation — vidi gore).
- Formalni Codex review (predstoji, brief pripremljen zasebno).
