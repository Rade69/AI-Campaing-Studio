# ACS-F1-010 — Claude (implementer) evidence report

- **Task:** ACS-F1-010 — SocialPostPayload persistence (`ContentPiece.payload` + migration)
- **Implementer:** claude (Human Owner decision, 2026-09-02)
- **Risk:** HIGH (SQLite/migrations) — Codex review + explicit Human Owner merge approval required
- **Status:** implementation complete, **not committed**

---

## Process note

I am both this project's coordinator and, for this one task, its implementer — by explicit
Human Owner assignment. Per `CLAUDE.md` ("Implementer != reviewer"), **I am not reviewing my own
work here.** This report is evidence for Codex + the Human Owner to review independently; I have
not marked this PASS, and I will not merge it myself the way MEDIUM tasks get merged under §29 —
that streamlined path is explicitly not available for HIGH-risk tasks regardless of who
implements them.

---

## Files changed

All in `allowed_paths`:

```
src/ai_campaign_studio/domain/content/entities.py                     (ContentPiece.payload — additive)
src/ai_campaign_studio/infrastructure/database/repositories/sqlite_content_repository.py  (payload_json read/write)
tests/integration/database/repositories/test_sqlite_content_repository.py  (3 new tests)
resources/migrations/0003_content_payload.sql                         (new)
```

Forbidden paths touched: **none** (`git status --short` below).

```
$ git status --short
 M src/ai_campaign_studio/domain/content/entities.py
 M src/ai_campaign_studio/infrastructure/database/repositories/sqlite_content_repository.py
 M tests/integration/database/repositories/test_sqlite_content_repository.py
?? resources/migrations/0003_content_payload.sql
```

`tests/integration/database/test_migrations.py` was **not** touched — read it first (per
contract) and confirmed its migration-count assertions are already tolerant of additional
migrations (`assert 0 in applied`, `len(rows) >= 1`), so no update was needed there. All 6 of its
existing tests still pass unmodified against the new 4-migration set.

---

## `domain/content/entities.py` diff (proof of additivity)

```diff
     facts_allowed: tuple[FactId, ...] = ()
     claims: tuple[ContentClaim, ...] = ()
     revision_ids: tuple[RevisionId, ...] = ()
+    payload: SocialPostPayload | None = None
 
     def __post_init__(self) -> None:
```

One trailing, optional field. Nothing else in the file changed — `SocialPostPayload` itself,
`__post_init__`, `CampaignTarget`, and every other field on `ContentPiece` are untouched.

---

## Migration

```sql
ALTER TABLE content_pieces ADD COLUMN payload_json TEXT;
```

One statement, nullable column, no `DROP`/`RENAME`/type change to any existing column. First
`ALTER TABLE` in the project (0000-0002 were all `CREATE TABLE` only) — verified the migration
runner (`migrations.py`) handles it with zero changes needed: it discovers `NNNN_name.sql` files
generically, splits on `;`, and applies inside a transaction. No runner code was touched.

---

## `sqlite_content_repository.py` — what changed

- `save_content_piece`: added `payload_json` to the INSERT/UPSERT column list and value tuple
  (`json.dumps(dataclasses.asdict(payload))` if `payload is not None` else `None`). No other
  column, no other statement (the `content_claims` DELETE+INSERT block is untouched).
- `_content_piece_from_row`: added `payload=SocialPostPayload(**json.loads(row["payload_json"]))
  if row["payload_json"] is not None else None`. No other field construction changed.
- `get_content_piece`, `list_campaign_content`, `_claim_from_row`: **untouched** (confirmed by
  diff below).

```
$ git diff --stat src/ai_campaign_studio/infrastructure/database/repositories/sqlite_content_repository.py
 1 file changed, 20 insertions(+), 3 deletions(-)
```

---

## Verification (doslovan output, `PYTHONPATH` override to this worktree)

```
$ python -c "import ai_campaign_studio"
(no output — success)
```

```
$ python -m pytest -q
442 passed in 75.83s
```

```
$ python -m pytest tests/integration/database/repositories/test_sqlite_content_repository.py tests/integration/database/test_migrations.py -v
14 passed in 1.72s
```

```
$ python -m pytest tests/architecture/test_import_boundaries.py -q
16 passed in 0.12s
```

```
$ python -m ruff check .
All checks passed!
```

```
$ python -m mypy src
Success: no issues found in 112 source files
```

---

## Acceptance checklist

- [x] `ContentPiece.payload` is the only new field — diff above proves it.
- [x] Round-trip test: populated `SocialPostPayload` (all fields, including `hashtags` tuple and
      `visual_direction`) survives save → get with dataclass `==` equality
      (`test_round_trip_content_piece_with_payload`).
- [x] Round-trip test: `payload=None` stays `None` after save → get, not an empty
      `SocialPostPayload` (`test_round_trip_content_piece_without_payload_stays_none`).
- [x] `save_content_piece` idempotent for `payload` changes too: re-save with a different payload
      updates the existing row (`COUNT(*) == 1`), doesn't duplicate
      (`test_save_content_piece_payload_update_is_idempotent`).
- [x] `test_migrations.py` passes unmodified against the new migration (no hardcoded migration
      count existed to begin with — verified by reading, not assumed).
- [x] Pre-existing `content_pieces`/`content_claims` tests (without `payload`) still pass
      unmodified — `payload_json` being `NULL` doesn't break rows that don't set it.
- [x] `pytest`, `ruff`, `mypy` all pass.
- [x] No changes outside `allowed_paths`.

---

## GitNexus

- Pre-change: provided in the Task Contract (`ContentPiece` upstream — tool-reported MEDIUM,
  explained as import-graph noise not semantic risk since every existing `ContentPiece`
  construction site uses keyword arguments; real blast radius is the one file in
  `allowed_paths`).
- Post-change `detect-changes`: **not run** — known worktree-binding limitation (see
  `.agent/CURRENT_STATE.md` blockers). Compensated with manual `git status`/`git diff` review
  (shown above) and by re-running the full test suite, which exercises every existing
  `ContentPiece` construction site.

---

## Not verified

- GitNexus `detect-changes` (see above).
- Live behavior beyond the test suite — this task has no GUI/CLI surface to launch.

---

## Waiting for

Codex review, then explicit Human Owner merge approval (HIGH-risk policy, no streamlined path).
Once merged, **ACS-F1-011 is unblocked** — Pi's worktree needs a `git merge main` before starting,
since it was branched before this task existed.
