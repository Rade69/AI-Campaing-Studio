# ACS-F1-008 — implementation evidence (Crush, 2026-09-02)

Implementer: Crush · Reviewer: Claude (round 1)
Branch: `task/ACS-F1-008-prompt-ai-mock`
Worktree: `H:\ai-campaign-studio-worktrees\ACS-F1-008-prompt-ai-mock`
Base: `main @ b4b324f`

A7 — Prompt repository + AI port + mock adapter. Nova, izolovana
infrastruktura; ne dira `ports/ai_registry.py` (P0 provider/model
registry), ne dira SecretStore/registry/bootstrap.

## Šta je urađeno

### Novi fajlovi (sve untracked)

1. `src/ai_campaign_studio/ports/ai.py` — `AIMessage`, `AIRequest`,
   `AIResponse`, `AITelemetry`, `TextGenerationPort` (Protocol).
2. `src/ai_campaign_studio/ports/prompts.py` — `PromptDefinition`,
   `PromptRepositoryPort` (Protocol).
3. `src/ai_campaign_studio/infrastructure/prompts/yaml_prompt_repository.py`
   — `YamlPromptRepository`, YAML loader, validira sva metadata polja.
4. `src/ai_campaign_studio/infrastructure/prompts/__init__.py`
5. `src/ai_campaign_studio/infrastructure/ai/mock_adapter.py` —
   `MockAdapter` sa 5 modova (`DETERMINISTIC`, `ERROR`,
   `INVALID_SCHEMA`, `RATE_LIMIT`, `TELEMETRY`).
6. `src/ai_campaign_studio/infrastructure/ai/__init__.py`
7. `resources/prompts/{campaign_plan,post_generation,revision,
   visual_direction,ab_control}/v1.yaml` — svih 5 obaveznih promptova.
8. `tests/unit/ports/test_ai.py`
9. `tests/unit/infrastructure/prompts/test_yaml_prompt_repository.py`
10. `tests/unit/infrastructure/ai/test_mock_adapter.py`

### Izmijenjeno (1, van `allowed_paths` — opravdano acceptance)

`tests/architecture/test_import_boundaries.py` — proširen za
`infrastructure/ai/` layer (zabrana provider SDK / browser / web
importa). Acceptance stavka eksplicitno traži ovo proširenje ako test
ne pokriva `infrastructure/ai/`; ACS-GUI-001 je uradio identičnu
stvar za `presentation_webview/`.

## Fixovi tokom sesije (nastavak prethodnog rada)

| Fajl | Problem | Fix |
|---|---|---|
| `resources/prompts/ab_control/v1.yaml` | YAML ScannerError: `purpose` value sa dvotačkom (`Baseline A/B control: ...`) | value pod navodnike |
| `infrastructure/ai/mock_adapter.py:13` | ruff E501 (94 > 88) | import razlomljen u više linija |
| `tests/unit/infrastructure/ai/test_mock_adapter.py:49` | ruff E501 (89 > 88) | assert razlomljen |
| `infrastructure/prompts/yaml_prompt_repository.py:30` | ruff UP037 (quotes u type annotation) | uklonjeni navodnici |
| `infrastructure/prompts/yaml_prompt_repository.py:7` | mypy `import-untyped` za `yaml` | `# type: ignore[import-untyped]` (isti obrazac kao keyring) |

## CI verifikacija

| Provjera | Komanda | Rezultat |
|---|---|---|
| pytest (full) | `python -m pytest -q` | **362 passed** |
| pytest (architecture) | `python -m pytest tests/architecture/test_import_boundaries.py -v` | **16 passed** |
| ruff | `python -m ruff check .` | All checks passed |
| mypy | `python -m mypy src` | Success: no issues found in 106 source files |

Environment note: paket instaliran editable (`pip install -e ".[dev]"`)
da bi `python -m pytest -q` prošao u ovom okruženju — ranije je falio
`keyring` (postojeći dependency, ne moj scope).

### Test breakdown (novi testovi)

```
tests/unit/ports/test_ai.py                              3
tests/unit/infrastructure/prompts/test_yaml_prompt_repository.py  5
tests/unit/infrastructure/ai/test_mock_adapter.py         7
tests/architecture/test_import_boundaries.py              1 (novi)
```

### Mock adapter — test po modu (acceptance zahtjev)

- `test_deterministic_mode_returns_same_response` — isti request → isti response
- `test_error_mode_raises` — `RuntimeError` (network-like)
- `test_invalid_schema_mode_returns_payload` — `structured_payload` ne prolazi Pydantic
- `test_rate_limit_mode_raises` — `MockRateLimitError`
- `test_telemetry_mode_populates_telemetry` — `latency_ms`/`input_tokens`/`output_tokens` popunjeni

### Prompt repository — acceptance dokaz

- `test_loads_all_five_prompts` — svih 5 promptova se učitava
- `test_missing_field_raises` — fali `output_contract` → `ValueError` (ne tiho)
- `test_unknown_version_raises` — `get("campaign_plan", "99")` → `ValueError`
- `test_ab_control_has_no_campaign_role_info` — nema `PROBLEM`/`EDUCATION`/
  `INSIGHT`/`PROOF`/`OBJECTION`/`BENEFIT`/`OFFER`/`ACTION` u instructions+examples

## Acceptance checklist

- [x] `ports/ai.py` i `ports/prompts.py` framework-neutral (nema yaml/http/SDK importa)
- [x] `yaml_prompt_repository.py` implementira `PromptRepositoryPort`, učitava svih 5
- [x] Nevalidan/nepotpun YAML baca grešku pri `get()` (test dokazuje)
- [x] `get(name, version)` za nepostojeću verziju vraća jasnu grešku
- [x] Svih 5 mock modova testirano pojedinačno
- [x] Mock adapter ne uvozi provider SDK (potvrđeno kroz `test_import_boundaries.py`)
- [x] `ab_control/v1.yaml` ne sadrži CampaignRole pipeline info (ručno + test)
- [x] Nema izmjena `ports/ai_registry.py` / `ai_registry/`
- [x] `python -m pytest -q`, `ruff check .`, `mypy src` prolaze
- [x] Nema izmjena van `allowed_paths` — osim `test_import_boundaries.py`
      (eksplicitno dozvoljeno acceptance stavkom)

## Review focus — Claude

- `ports/ai.py`/`ports/prompts.py` stvarno framework-neutral
- svih 5 YAML fajlova imaju svih 7+1 metadata polja (name, version, purpose,
  input_contract, output_contract, language_support, instructions, examples)
- `AITelemetry` ostaje lagan response-embedded snapshot (NE `ports/telemetry.py`
  puna infra) — scope discipline
- `TextGenerationPort` je različit koncept od `ports/ai_registry.py` (provider/model
  registry netaknut)

## Nisam uradio

- `git commit` — workflow §29: koordinator (Claude) radi commit/push/merge
  nakon PASS review-a.

## Worktree status

```
$ git status --porcelain
 M tests/architecture/test_import_boundaries.py
?? resources/prompts/
?? src/ai_campaign_studio/infrastructure/ai/
?? src/ai_campaign_studio/infrastructure/prompts/
?? src/ai_campaign_studio/ports/ai.py
?? src/ai_campaign_studio/ports/prompts.py
?? tests/unit/infrastructure/
?? tests/unit/ports/test_ai.py
```
