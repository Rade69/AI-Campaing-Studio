# AI Campaign Studio — example configuration.
#
# Schema lives in `src/ai_campaign_studio/config/settings.py` (AppSettings).
# This file is a tracked example, not read at runtime in this phase.

app_name = "AI Campaign Studio"
environment = "development"          # development | test | production
log_level = "INFO"                   # DEBUG | INFO | WARNING | ERROR | CRITICAL
app_locale = "BHS_LATIN"             # EN | BHS_LATIN
database_filename = "ai_campaign_studio.db"

# Optional directory overrides (uncomment to use):
# data_dir_override = "/path/to/data"
# resource_dir_override = "/path/to/resources"
